import { PopupService, PopupRef } from '@progress/kendo-angular-popup';
import { Renderer2, NgZone, OnDestroy } from '@angular/core';
import { PreventableEvent } from '../../common/preventable-event';
import { Subject } from 'rxjs/Subject';
import { LocalizationService } from '@progress/kendo-angular-l10n';
import { ScrollSyncService } from '../../scrolling/scroll-sync.service';
/**
 * @hidden
 */
export declare class SinglePopupService implements OnDestroy {
    private popupService;
    private renderer;
    private ngZone;
    private localization;
    readonly onClose: Subject<PreventableEvent>;
    private removeClick;
    private popupRef;
    private scrollSubscription;
    private popupViewPortLeaveSubscription;
    constructor(popupService: PopupService, renderer: Renderer2, ngZone: NgZone, scrollSyncService: ScrollSyncService, localization: LocalizationService);
    open(anchor: any, template: any, popupRef: PopupRef): PopupRef;
    destroy(): void;
    ngOnDestroy(): void;
    private detachClose();
    private attachClose(skipElement);
}
