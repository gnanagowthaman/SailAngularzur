import { ChangeDetectorRef, EventEmitter, OnChanges, OnDestroy, TemplateRef } from '@angular/core';
import { CellContext } from './models/cell-context.interface';
import { ViewService } from './models/view-service.interface';
import { CalendarViewEnum } from './models/view.enum';
import { BusViewService } from './services/bus-view.service';
import { IntlService } from '@progress/kendo-angular-intl';
/**
 * @hidden
 */
export declare class ViewComponent implements OnChanges, OnDestroy {
    bus: BusViewService;
    intl: IntlService;
    private cdr;
    isActive: boolean;
    activeView: CalendarViewEnum;
    cellUID: string;
    focusedDate: Date;
    selectedDate: Date;
    viewDate: Date;
    min: Date;
    max: Date;
    weekNumber: boolean;
    viewIndex: number;
    templateRef: TemplateRef<any>;
    weekNumberTemplateRef: TemplateRef<any>;
    change: EventEmitter<Date>;
    colSpan: number;
    data: CellContext[][];
    service: ViewService;
    title: string;
    private showWeekNumbers;
    private today;
    private intlSubscription;
    constructor(bus: BusViewService, intl: IntlService, cdr: ChangeDetectorRef);
    ngOnChanges(_: any): void;
    ngOnDestroy(): void;
    handleClick(ctx: CellContext): void;
    firstDate(rowCtx: CellContext[]): Date;
    getWeekNumber(date: Date): number;
    getWeekNumberContext(rowCtx: CellContext[]): CellContext;
    getStyles(context: CellContext): any;
    private firstWeekDateContext(rowCtx);
    private updateData();
    private intlChange();
}
