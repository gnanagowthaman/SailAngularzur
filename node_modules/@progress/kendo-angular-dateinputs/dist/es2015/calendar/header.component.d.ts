import { ChangeDetectorRef, EventEmitter, OnChanges, OnDestroy } from '@angular/core';
import { LocalizationService } from '@progress/kendo-angular-l10n';
import { IntlService } from '@progress/kendo-angular-intl';
import { CalendarViewEnum } from './models/view.enum';
import { BusViewService } from './services/bus-view.service';
/**
 * @hidden
 */
export declare class HeaderComponent implements OnChanges, OnDestroy {
    private bus;
    private changeDetector;
    localization: LocalizationService;
    navigate: boolean;
    todayIsInRange: boolean;
    todayMessage: string;
    title: string;
    activeView: CalendarViewEnum;
    currentDate: Date;
    min: Date;
    max: Date;
    today: EventEmitter<Date>;
    readonly getComponentClass: boolean;
    private intlSubscription;
    constructor(bus: BusViewService, changeDetector: ChangeDetectorRef, localization: LocalizationService, intl: IntlService);
    ngOnChanges(_: any): void;
    ngOnDestroy(): void;
    handleTodayClick(): void;
    handleNavigation(): void;
    private intlChange();
}
